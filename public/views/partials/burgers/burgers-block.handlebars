<li>
	{{burger_name}}

	<button class="devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{!-- {{#if }}SLEEP TIME!{{else}}WAKE UP!{{/if}} --}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
